# 📢 광고업계 트렌드 분석 시스템

광고/미디어/매체 관련 최신 트렌드와 연구를 자동으로 수집·분석하고 한국어 보고서를 이메일로 발송하는 시스템입니다.

## 📋 정보 수집 소스

### 업계 뉴스 (RSS 피드)
- **AdAge** - 광고업계 종합 뉴스
- **Adweek** - 캠페인·트렌드 분석
- **The Drum** - 마케팅 기술·전략
- **Marketing Dive** - 마케팅 산업 동향
- **Digiday** - 디지털 광고·프로그래매틱
- **MediaPost** - 미디어·광고 종합

### 학술 연구 (API)
- **Semantic Scholar** - 2억+ 논문 DB
- **arXiv** - CS/경제학 프리프린트

## 🚀 사용법

### 1. 의존성 설치
```bash
pip install -r scripts/requirements.txt
```

### 2. Gmail 앱 비밀번호 설정 (이메일 발송용)
```bash
# Windows
set GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# PowerShell
$env:GMAIL_APP_PASSWORD="xxxx xxxx xxxx xxxx"
```

> 앱 비밀번호 생성: https://myaccount.google.com/apppasswords

### 3. 실행
```bash
# 전체 실행 (수집 + 분석 + 이메일)
python scripts/run_daily.py

# 이메일 없이 실행
python scripts/run_daily.py --no-email

# 디버그 모드
python scripts/run_daily.py --debug
```

## 📁 폴더 구조

```
ad-research/
├── scripts/           # 실행 스크립트
│   ├── config.py          # 설정 (소스, 키워드, 이메일)
│   ├── collect_trends.py  # 트렌드/논문 수집
│   ├── analyze_and_report.py  # 분석 + 보고서 생성
│   ├── send_email.py      # 이메일 발송
│   ├── run_daily.py       # 메인 파이프라인
│   └── requirements.txt
├── data/              # 날짜별 수집 데이터
│   └── YYYY-MM-DD/
├── reports/           # 한국어 보고서
└── README.md
```

## ⏰ 자동 실행 (Windows 작업 스케줄러)

매일 자동 실행하려면 Windows 작업 스케줄러에 등록하세요:

1. `Win + R` → `taskschd.msc` 실행
2. "기본 작업 만들기" 선택
3. 트리거: 매일, 원하는 시간
4. 동작: 프로그램 시작
   - 프로그램: `python`
   - 인수: `scripts/run_daily.py`
   - 시작 위치: 이 프로젝트 폴더 경로
